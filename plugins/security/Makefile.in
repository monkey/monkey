CC 	= $CC
CFLAGS	= $CFLAGS
DEFS    = $DEFS
INCDIR  = ../../src/include

all:
	$(CC) $(CFLAGS) $(DEFS) -I$(INCDIR) -fPIC -c security.c
	$(CC) $(CFLAGS) $(DEFS) -shared -o monkey-security.so security.o -lc
clean:
	rm -rf *.o
	rm -rf *.*so*
