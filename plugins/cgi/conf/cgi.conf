# CGI configuration
# ==================

# Give an extended regex for matching the URL of allowed executables.
# You can list as many as needed, space-separated.

[CGI]
    Match /cgi-bin/.*\.cgi .*\.sh
