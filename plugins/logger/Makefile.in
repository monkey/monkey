CC 	= $CC
CFLAGS	= $CFLAGS
DEFS    = $DEFS
INCDIR  = ../../src/include

all:
	gcc $(CFLAGS) $(DEFS) -I$(INCDIR) -fPIC -c logger.c
	gcc $(CFLAGS) $(DEFS) -shared -o monkey-logger.so logger.o -lc
clean:
	rm -rf *.o *~
	rm -rf *.*so*
