CC 	= $CC
CFLAGS	= $CFLAGS
DEFS    = $DEFS
INCDIR  = ../../src/include

all:
	$(CC) $(CFLAGS) $(DEFS) -I$(INCDIR) -fPIC -c liana_ssl.c -lc /usr/lib/libmatrixssl.a
	$(CC) $(CFLAGS) $(DEFS) -shared -o monkey-liana_ssl.so liana_ssl.o -lc /usr/lib/libmatrixssl.a
clean:
	rm -rf *.o *~ *.*so*
