CC 	= $CC
CFLAGS	= $CFLAGS
DEFS    = $DEFS
INCDIR  = ../../src/include

all:
	$(CC) $(CFLAGS) $(DEFS) -I$(INCDIR) -fPIC -c dirlisting.c
	$(CC) $(CFLAGS) $(DEFS) -shared -Wl -o monkey-dirlisting.so dirlisting.o -lc
clean:
	rm -rf *.o
	rm -rf *.*so*
