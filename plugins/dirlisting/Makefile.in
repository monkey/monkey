CC 	= gcc
CFLAGS	= $CFLAGS
DEFS    = $DEFS
INCDIR  = ../../src/include

all:
	gcc $(CFLAGS) $(DEFS) -I$(INCDIR) -fPIC -c dirlisting.c
	gcc $(CFLAGS) $(DEFS) -shared -Wl -o monkey-dirlisting.so dirlisting.o -lc
clean:
	rm -rf *.o
	rm -rf *.*so*
