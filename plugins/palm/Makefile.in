CC 	= $CC
CFLAGS	= $CFLAGS
DEFS    = $DEFS
INCDIR  = ../../src/include
SOURCES = palm.c cgi.c request.c

all:
	$(CC) $(CFLAGS) $(DEFS) -I$(INCDIR) -fPIC -c $(SOURCES)
	$(CC) $(CFLAGS) $(DEFS) -shared -o monkey-palm.so cgi.o palm.o request.o -lc
clean:
	rm -rf *.o
	rm -rf *.*so*
