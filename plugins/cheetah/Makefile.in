CC 	= $CC
CFLAGS	= $CFLAGS
DEFS    = $DEFS
INCDIR  = ../../src/include

all:
	$(CC) $(CFLAGS) $(DEFS) -I$(INCDIR) -fPIC -c cheetah.c
	$(CC) $(CFLAGS) $(DEFS) -shared -Wl -o monkey-cheetah.so cheetah.o -lc
clean:
	rm -rf *.o
	rm -rf *.*so*
